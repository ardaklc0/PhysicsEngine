#include "euler_method_multi_variable.h"
#include <iostream>
using namespace cyclone;
using namespace std;
#include <cmath>

float EulerMethodMultiVariable::solve(int n, float(*func)(float, float)) const
{
	for (int i = 0; i < n; i++) {
		return 0;
	}
	return 0;
}
